/target
/out
/dist
.roc-fuzz/
/typedTarget
/failureArtifact
/.test-cache
/.docs-out
*.ll
*.bc
*.o
*.a

# Linker inputs are built only by the explicit producer or restored from the
# content-hashed release cache. Keep target documentation, never payloads.
/platform/targets/*/SHA256SUMS
/platform/targets/*/NATIVE_LIBRARIES.json
/examples/**/main
