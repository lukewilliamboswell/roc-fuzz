name: CI

on:
  pull_request:
  push:
    branches: [trunk]
  workflow_dispatch:
    inputs:
      nightly_validation:
        description: Validate a nightly candidate without publication
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  native-sanitizers:
    name: Native sanitizers
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout roc-fuzz
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install stable tooling Roc and latest nightly
        id: roc
        uses: lukewilliamboswell/setup-roc@89a8a2945bd107f27b160096b3af7f52d2d78369 # stable-and-nightly fork
        with:
          version: stable-and-nightly
          stable-tag: nightly-2026-09-18-1d982dc
          nightly-tag: nightly-2026-09-19-d025939

      - name: Build and run the pinned libFuzzer with AddressSanitizer
        env:
          ROC_STABLE: ${{ steps.roc.outputs.stable-executable }}
        run: |
          "$ROC_STABLE" --opt=dev scripts/test_native_sanitizers.roc

  published-examples:
    name: Public downloads (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, macos-26]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - name: Install stable tooling Roc and latest nightly
        id: roc
        uses: lukewilliamboswell/setup-roc@89a8a2945bd107f27b160096b3af7f52d2d78369 # stable-and-nightly fork
        with:
          version: stable-and-nightly
          stable-tag: nightly-2026-09-18-1d982dc
          nightly-tag: nightly-2026-09-19-d025939
      - name: Test committed URLs from a fresh cache
        if: runner.os != 'macOS'
        env:
          ROC_STABLE: ${{ steps.roc.outputs.stable-executable }}
          ROC_NIGHTLY: ${{ steps.roc.outputs.nightly-executable }}
        run: >-
          "$ROC_STABLE" --opt=dev scripts/test_targets.roc --
          --operation all --max-total-time 1 --verbose

      - name: Type-check the target runner on macOS
        if: runner.os == 'macOS'
        env:
          ROC_STABLE: ${{ steps.roc.outputs.stable-executable }}
        run: |
          "$ROC_STABLE" check scripts/test_targets.roc

  self-contained-runner:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            platform-target: x64musl
          - os: macos-26
            platform-target: arm64mac
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout roc-fuzz
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Read workspace dependencies
        id: dependencies
        run: echo "zig-version=$(jq -r .zig_version scripts/workspace-deps.json)" >> "$GITHUB_OUTPUT"

      - name: Install stable tooling Roc and latest nightly
        id: roc
        uses: lukewilliamboswell/setup-roc@89a8a2945bd107f27b160096b3af7f52d2d78369 # stable-and-nightly fork
        with:
          version: stable-and-nightly
          stable-tag: nightly-2026-09-18-1d982dc
          nightly-tag: nightly-2026-09-19-d025939

      - name: Install Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2
        with:
          version: ${{ steps.dependencies.outputs.zig-version }}

      - name: Show tool versions
        run: |
          "$ROC_STABLE" version
          "$ROC_NIGHTLY" version
          zig version
        env:
          ROC_STABLE: ${{ steps.roc.outputs.stable-executable }}
          ROC_NIGHTLY: ${{ steps.roc.outputs.nightly-executable }}

      - name: Resolve the reviewed linker-input identity
        id: linker-inputs
        env:
          TARGET: ${{ matrix.platform-target }}
        run: echo "identity=$(python3 scripts/link_input_artifacts.py cache-identity --target "$TARGET")" >> "$GITHUB_OUTPUT"

      - name: Restore the content-addressed linker-input download cache
        if: steps.linker-inputs.outputs.identity != 'bootstrap'
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: .test-cache/native-libraries
          key: linker-inputs-${{ matrix.platform-target }}-${{ steps.linker-inputs.outputs.identity }}

      - name: Prepare host and reviewed linker inputs
        env:
          ROC_STABLE: ${{ steps.roc.outputs.stable-executable }}
          ROC_NIGHTLY: ${{ steps.roc.outputs.nightly-executable }}
          TARGET: ${{ matrix.platform-target }}
          INPUT_IDENTITY: ${{ steps.linker-inputs.outputs.identity }}
        run: |
          if [ "$INPUT_IDENTITY" = bootstrap ]; then
            scripts/build_platform.roc -- --target "$TARGET" --libraries source
          else
            scripts/build_platform.roc -- --target "$TARGET"
          fi

      - name: Validate the repository
        env:
          ROC_STABLE: ${{ steps.roc.outputs.stable-executable }}
          ROC_NIGHTLY: ${{ steps.roc.outputs.nightly-executable }}
        run: scripts/check_all.roc

      - name: Test examples against the working-tree platform
        env:
          ROC_STABLE: ${{ steps.roc.outputs.stable-executable }}
          ROC_NIGHTLY: ${{ steps.roc.outputs.nightly-executable }}
        run: |
          scripts/build_bundle.roc -- --target ${{ matrix.platform-target }} --output-dir .test-cache/ci-bundle
          scripts/test_bundle.roc -- .test-cache/ci-bundle/*.tar.zst

      - name: Generate and validate API documentation
        env:
          ROC_DOCS_URL_ROOT: /roc-fuzz/main
          ROC_STABLE: ${{ steps.roc.outputs.stable-executable }}
          ROC_NIGHTLY: ${{ steps.roc.outputs.nightly-executable }}
        run: |
          "$ROC_NIGHTLY" docs --output=.test-cache/docs platform/main.roc
          scripts/check_docs.roc -- .test-cache/docs

  required:
    name: CI required
    if: always()
    needs: [native-sanitizers, self-contained-runner, published-examples]
    runs-on: ubuntu-24.04
    permissions: {}
    steps:
      - name: Require every CI matrix job
        env:
          MATRIX_RESULT: ${{ needs.self-contained-runner.result }}
          SANITIZER_RESULT: ${{ needs.native-sanitizers.result }}
          PUBLIC_RESULT: ${{ needs.published-examples.result }}
        run: |
          test "$MATRIX_RESULT" = success
          test "$SANITIZER_RESULT" = success
          test "$PUBLIC_RESULT" = success
